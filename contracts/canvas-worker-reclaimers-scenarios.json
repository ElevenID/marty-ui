{
  "schema": "marty.canvas-worker-reclaimers-scenarios/v1",
  "extends": "canvas-worker-provider-final-scenarios.json",
  "concurrency_scenario": "canvas-worker-concurrent-scenarios.json",
  "reclaimer_ids": ["worker-rest", "worker-contender"],
  "barrier_sql": "LOCK TABLE issuance_service.canvas_evidence_sync_jobs IN ACCESS EXCLUSIVE MODE",
  "blocked_reclaimers_sql": "SELECT count(*) FROM pg_stat_activity WHERE datname=current_database() AND pid<>pg_backend_pid() AND state='active' AND wait_event_type='Lock' AND query LIKE '%canvas_evidence_sync_jobs%'",
  "scope": "Actual attempt-eight renewal and SIGKILL from the final-attempt scenario, then real lease expiry and two actual reclaimer processes observed waiting at an isolated job-table barrier. Require one dead-letter, disabled target, no additional provider request, both fresh idle heartbeats and process liveness. Earlier attempts remain historical seed before first worker startup. No changed-target or final-completion race claim."
}
