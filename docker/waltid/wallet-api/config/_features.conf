enabledFeatures = [
    dev-mode    # enables dev-mode.conf overrides; safe for local playwright testing
]
disabledFeatures = [
    # auth  # uncomment to disable legacy auth (not recommended for local testing)
]
