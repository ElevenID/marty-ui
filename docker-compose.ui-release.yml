name: elevenid-beta-ui

services:
  ui-prod:
    image: ${MARTY_UI_RELEASE_IMAGE:?MARTY_UI_RELEASE_IMAGE must be set}
    restart: unless-stopped
    ports:
      - "3002:80"
    networks:
      - default
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:80/"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s

networks:
  default:
    external: true
    name: ${MARTY_NETWORK_NAME:-elevenid-beta-network}
